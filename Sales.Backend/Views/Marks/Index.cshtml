@model IEnumerable<Sales.Common.Models.Mark>

@{
    ViewBag.Title = "Listado";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MarkName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MarkName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MarkId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MarkId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MarkId })
        </td>
    </tr>
}

</table>
